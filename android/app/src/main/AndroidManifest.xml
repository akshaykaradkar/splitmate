<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ (API 30+) Package Visibility for NPCI UPI Deep-Linking & Installed UPI App Discovery -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" android:host="pay" />
        </intent>
        <package android:name="com.google.android.apps.nbu.paisa.user" /> <!-- Google Pay (GPay) -->
        <package android:name="com.phonepe.app" />                        <!-- PhonePe -->
        <package android:name="net.one97.paytm" />                        <!-- Paytm -->
        <package android:name="in.org.npci.upiapp" />                     <!-- BHIM UPI -->
        <package android:name="com.dreamplug.androidapp" />               <!-- CRED UPI -->
        <package android:name="in.amazon.mShop.android.shopping" />       <!-- Amazon Pay -->
        <package android:name="com.whatsapp" />                           <!-- WhatsApp UPI -->
    </queries>

    <application
        android:name=".SplitMateApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="SplitMate"
        android:supportsRtl="true"
        android:theme="@style/Theme.SplitMate">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
